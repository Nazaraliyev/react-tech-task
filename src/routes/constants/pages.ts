const pages = {
  auth: '',
  customers: '/customers',
  credits: '/credits',
  guarantors: '/guarantors',
  notFound: '/404',
};

const route = {
  auth: '/auth',
  app: '/',
};

export { route };
export default pages;
